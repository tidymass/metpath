% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_feature_based_pathway_enrichment.R
\name{annotate_metabolites_fpa}
\alias{annotate_metabolites_fpa}
\title{Annotate Metabolites for Feature Pathway Analysis (FPA)}
\usage{
annotate_metabolites_fpa(
  feature_table,
  metabolite_database,
  column = c("rp", "hilic"),
  adduct.table = NULL,
  ms1.match.ppm = 10,
  rt.match.tol = 5,
  mz.ppm.thr = 400,
  threads = 3
)
}
\arguments{
\item{feature_table}{A data frame containing feature data, including `variable_id`, `mz`, `rt`, and `polarity` columns.}

\item{metabolite_database}{A metabolite database object used for annotation.}

\item{column}{A character string specifying the chromatography column type. Options are `"rp"` (reverse phase) or `"hilic"` (hydrophilic interaction chromatography). Default is `"rp"`.}

\item{adduct.table}{Optional data frame specifying possible adducts. Default is `NULL`.}

\item{ms1.match.ppm}{Numeric; mass tolerance for MS1 matching in parts per million (ppm). Default is `10`.}

\item{rt.match.tol}{Numeric; retention time tolerance threshold for matching in seconds. Default is `5`.}

\item{mz.ppm.thr}{Numeric; M/Z tolerance threshold for filtering in ppm. Default is `400`.}

\item{threads}{Integer; number of parallel threads to use for annotation. Default is `3`.}
}
\value{
A data frame containing the annotated metabolites with polarity information.
}
\description{
This function performs metabolite annotation for a given feature table using an MS1-based approach.
It processes both positive and negative ionization modes separately and extracts annotation results.
}
